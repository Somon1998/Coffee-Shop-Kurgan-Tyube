# Coffee Shop

## Development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

Notes:
- Images are in `public/img`.
- Styles are bundled via `src/styles/main.scss` and imported from `src/main.js`.
